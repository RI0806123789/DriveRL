# DriveRL の環境変数（すべて任意）
#
# このファイルを .env としてコピーすれば backend/app/config.py が読み込む。
# 指定しなければ括弧内の既定値が使われる。

# バックエンドの待ち受けアドレス（既定: 127.0.0.1）
DRIVERL_HOST=127.0.0.1

# バックエンドの待ち受けポート（既定: 8000）
# 変更したら frontend/vite.config.ts のプロキシ先も合わせること
DRIVERL_PORT=8000
