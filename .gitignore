# -- dev --
/memo
CLAUDE.md

# --- モデルの重みファイル（万一 memo/ 外に置かれた場合の保険） ---
*.pt
*.keras
*.torchscript.pt


# --- Python (backend) ---
backend/.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# --- 生成データ（OSM キャッシュ・学習チェックポイント・書き出し・認識器）---
# ★ ディレクトリを 1 つずつ並べないこと（code_review Q-13）。以前は 5 つを列挙して
#   おり、画像認識ベースへの移行で増えた `detector/`（.keras 1.97MB +
#   dataset の .npz 2.59MB）だけが漏れていた。config.py が起動時に
#   `backend/data/` 配下を全部作るので、空ディレクトリを残す必要は無い。
backend/data/

# --- Node (frontend) ---
frontend/node_modules/
frontend/dist/
*.local

# --- 環境変数 ---
.env

# --- エディタ / OS ---
.vscode/
.idea/
Thumbs.db
Desktop.ini
