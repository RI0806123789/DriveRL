# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

DriveRL は、OSM の実地図上でマルチエージェント強化学習（自作 PPO）の車両を走らせ、
Three.js で 3D 描画・介入できるシミュレーターです。**車両は擬似カメラ画像を CNN で認識した
結果だけを見て走ります。**

コード内のコメント・ドキュメント・ユーザーとのやり取りはすべて**日本語**です。
変数名・識別子は英語。

リポジトリ直下の `AGENTS.md` は、Claude Code 以外のエージェント（Codex / Copilot /
Cursor など）に向けた同じ案内です。**下の「作業の進め方」は AGENTS.md と
一字一句同じものを置いてあります。片方を直したら必ずもう片方も直すこと。**

---

## コマンド

Python は `backend/.venv`、Node は `frontend/`。ルートの `run.ps1` / `run.cmd` は
venv のパスを埋めるだけの薄いラッパーで、中身は `backend/run.py` です。

```powershell
# 起動（-Dev で Vite も子プロセスとして面倒を見る → localhost:5173）
.\run.ps1 -Dev          # 開発中。フロントの変更が保存だけで反映される
.\run.ps1               # バックエンドのみ（frontend/dist を配信）→ 127.0.0.1:8000
.\run.ps1 -Build        # ビルドしてから上

# フロントエンド
cd frontend
npm run typecheck       # tsc --noEmit
npm run build           # typecheck + vite build
npm run verify          # 幾何検証をまとめて実行（ブラウザ不要。本数の出典は package.json）
npm run verify:signals  # 1 本だけ（他: speedsigns / camera / colors / vehicles / sun /
                        #             detections / markings / weather / pedestrian / taximap）
npm run icons           # PWA アイコンを再生成（public/icon-*.png。手で描かない）
npm run dev             # Vite だけ立てる。?mock=1 でバックエンド無しでも動く（後述）

# バックエンド（backend/ から。`python -m app.x` なら PYTHONPATH は不要）
.venv\Scripts\python.exe -m app.map.prefetch          # OSM の事前ダウンロード（初回は数十秒/エリア）
.venv\Scripts\python.exe train_detector.py --samples 2400 --epochs 12   # 認識器の学習
.venv\Scripts\python.exe train_detector.py --collect-only              # 教師データ収集だけ
# ↑ どちらも操作パネルの「モデル作成」タブから同じことができる（中身は同じ実装）
.venv\Scripts\python.exe verify_log_std.py           # 方策分布の健全性チェック
.venv\Scripts\python.exe warmstart_policy.py --preset ginza  # 「止まる」に固まった方策を立て直す
```

**`-Dev` でリロードするのは Vite だけです。** バックエンドを直したら Ctrl+C で止めて
起動し直す必要があります（`--reload` を使わないのは、学習スレッドが作り直されるため）。

### テスト

**自動テストのフレームワークは入っていません**（pytest も vitest も無い）。確認手段は
`npm run verify` の幾何検証スクリプト群と、型チェック・ビルド、そして実測です。
バックエンドの変更を検証するときは、スクラッチにベンチ／比較スクリプトを書いて
**新旧の結果が一致することと速度**を数値で確かめるのが、このリポジトリで機能している作法です。

`npm run verify` が存在する理由: **3D の向きは間違っていても型チェックもビルドも通る**ため、
幾何計算を React から切り離した純粋モジュールへ置き、数値で不変条件を検査しています
（実際に灯火の並びが左右逆になっていたバグをこれが検出しました）。
Node 22.18 以降が要るのは `node scripts/verify-*.ts` で TS を直接実行するためです。

★ **本数は `frontend/package.json` の `scripts` が唯一の出典です。** 増やしたら
ここと `README.md`「検証」節の両方を直すこと。過去に **CLAUDE.md が 7 本 /
README.md が 4 本 / 実際が 8 本**という三つ巴のずれを起こしました
（手で書いた数字は必ず古くなる、という `code_review` Q-15 と同じ型）。

---

## アーキテクチャ

### 2 つの契約ファイル

| ファイル | 何の契約か |
|---|---|
| `docs/protocol.md` | **フロント ↔ バックの唯一の契約。** WebSocket メッセージ・座標系。破壊的変更時は `protocolVersion` を上げる |
| `backend/app/contracts.py` | **バックエンド内部の契約。** 依存を持たない中立地帯で、`map` / `sim` / `rl` / `runtime` はここ経由でのみやり取りする |

`frontend/src/types/protocol.ts` は `docs/protocol.md` の TypeScript 版です。
**3 つはセットで直すこと。**

### 観測が作られる経路（ここが一番間違えやすい）

```
world（真値）
  ├─ percep/weather.py    天候（雨・霧）。**視程はここが唯一の出典**
  │      ↓ 視程
  ├─ percep/camera.py     擬似カメラ 192×144 を numpy で描く（運転席視点）
  │      ↓ 画像
  ├─ percep/detector.py   CNN（Keras 3 / torch バックエンド。TensorFlow は入れない）
  │      ↓ 検出結果 (percep/types.py)
  ├─ percep/encoder.py    → 観測 66 次元
  └─ percep/groundtruth.py  真値から作る「理想の検出結果」
                            ★ 教師データ兼フォールバックの二役。分けてはいけない
```

収集の前に**いまの認識器を採点する**のが `percep/evaluate.py` です（⑩ アクティブ
ラーニング）。真値と CNN の検出を突き合わせ、クラス別・天候別の成績を出し、
**その成績がそのまま次の収集の重みになります**（成績が低いほど多く集める）。
採点は収集の直前に 1 度だけ走り、**走行中のステップには一切乗りません**
（走行を重くしないため。50ms の予算を守る）。

認識器の**学習**は `percep/trainer.py` にあり、**CLI（`train_detector.py`）と
Web UI（`runtime/detector_job.py` →「モデル作成」タブ）が同じ関数を呼ぶ**。
収集・損失・検証を片方にだけ足すと「コマンドでは学習できるのに画面からだと違う」
という切り分け不能な食い違いになるので、**アルゴリズムは必ず `trainer.py` へ書くこと。**
値域（枚数・エポック・バッチ・倍率・乱数種）は `runtime/detector_job.py` の定数が
唯一の出典で、CLI も UI もそこを参照します。

教師データまわりの不変条件（どれも**成績が良くなる方向に症状が出る**ので外から気づけません）:

- **画像は `uint8` のまま学習へ渡す。** `build_detector()` が入力直後に
  `Rescaling(1/255)` を持っています。学習側で float32 に広げると 4,800 枚で
  380MB が 1,519MB になり、**出力は 1 ビットも変わりません**（実測で最大差 0.000e+00）
- **`collect_dataset` は返す直前に全体を並べ替える。** Keras の `validation_split` は
  shuffle より**前に末尾から**切り出すので、時系列順のまま渡すと検証データが
  「収集の最後の 1.5 秒ぶんの画」になり、20Hz の連続フレームは隣同士がほぼ同じなので
  **学習データとほぼ重複します**（val_loss が実力より低く出る）
- **散らし直しの周期は「枚数」ではなく「ステップ数」。** 1 回の `render()` で
  アクティブな台数ぶん（既定 8 枚）まとめて貯まるので、2,400 枚は 300 ステップです。
  `_reset_interval()` が `samples` から決めます。**寄せ直したらパイロンも同時に置き直すこと**
  （以前は別々の周期に任せていて、片方を変えると黙って崩れる形になっていました）
- **認識器の学習中は `load_map` を受け付けない。** ジョブは開始時に借りた `MapIndex` を
  握り続けるので、通すと「収集は元のエリア・画面は新しいエリア」になります
- **弱点を狙わない（`class_focus` が空の）ときは、狙うクラスを選ばない。**
  一様に選ぶと信号・標識狙いの寄せ（`_place_facing`）が毎回 1/5 で走り、似た構図
  ばかりになって過学習します（実測: 銀座 1,600 枚 12 エポックで val_loss が学習損失の
  50 倍、検出率 9.3%）。`collect_dataset` の `pick_focus()` がこれを守っています
- **弱点を狙う強さ（`evaluate.FOCUS_GAIN`）を上げすぎない。** 3.0 で回したとき、
  霧は 11.5% → 65.4% になった代わりに**晴れが 79.4% → 63.0% へ落ちました**
  （2,400 枚のうち晴れが 304 枚 = 12.7% しか残らなかったため）。1.5 に下げると
  晴れ 65.1% / 雨 70.2% / 霧 71.3% で釣り合います
- ★ **パイロンと歩行者は `scatter_props()` の 1 本から置き直すこと。** 別々に呼べる
  ようにしておくと、片方だけ呼ぶ経路がいつか生まれ、狙ったクラスが薄いまま集まります。
- ★ **歩行者は街へ一様に撒かない。** 金沢は 12.3km 四方なので、64 人を一様に散らすと
  1 枚の画に 1 人も写りません（実測で真値 0 件）。`scatter_pedestrians()` が走っている
  車の周りへ寄せます。**寄せる範囲を狭めすぎないこと**で、26m に寄せたときは歩行者が
  他クラスを押しのけ（`encode_targets` は 6×8 の 1 セルに 1 物体しか置けない）、
  銀座 240 枚で車両 234 → 163 件・1 枚あたりの物体 6.01 → 5.55 個に減りました。
  既定は 115m（狙うときだけ 26m）です。
- ★ **パイロンを濃く置くかどうかを決めるのは `scatter_obstacles(focus=...)` の中だけ。**
  呼び出し側で `dense` を組み立てないこと。以前は `arrange_scene()` が濃く置いたものを、
  40 ステップごとの散らし直し（`SCATTER_EVERY`）が**狙いを知らないまま素に戻して**いて、
  既定の 4,800 枚では **600 ステップ中 278 ステップ（46.3%）が薄いまま**でした
  （銀座・8 台で 24 本 → 8 本。`code_review` T-02）。**寄せ直しとパイロンを
  別々の周期に任せない**という上の約束は、狙うクラスについても同じです。

### 街を歩く歩行者（`sim/pedestrians.py`）

`PedestrianCrowd` が最大 64 人を numpy でまとめて進めます。人数は `pedestrianCount`
（`docs/protocol.md` 2.5）で、擬似カメラ・正解ラベル・観測・衝突判定のすべてに効きます。

★ **これらが見る一覧は `World.pedestrian_xy` で、`crowd` そのものではありません。**
実用モードの徒歩キャラ（`World.set_player()`）がここで並びます。**新しく歩行者を
見たい処理を足すときも、必ずこのプロパティを通すこと**（`crowd.positions` を直接
読むと、利用者だけが見えない処理が静かに増えます）。

- ★ **状態は「どのエッジの歩道を、どちら向きに、左右どちら側を歩いているか」で持つこと。**
  座標を直接積分すると建物へめり込み、道路から離れていきます。位置は毎ステップ
  `_refresh_pose()` がエッジ上の状態から作り直します（**外から x/y を書いても次の
  ステップで消えます**）。
- ★ **`MapIndex.nearest_road_point()` の 4 番目の戻り値は接線方位であって弧長ではない。**
  そのまま弧長として使うと、置いたつもりの場所とまったく違う所に立ちます
  （実測で「車の 30m 前方」に置いたつもりが 6.5m 後方に出た）。
  弧長は `SidewalkNetwork.arc_of()` が出します。
- ★ **どの車からも 220m 以上離れた歩行者は、車の近くへ回すこと**（`recycle()`）。
  無いと広いマップで 1 人も画に写りません（金沢 12.3km 四方に 64 人 ＝ 0.42 人/km²。
  実測で真値 0 件 → 回すようにして 5 件）。誰にも見えていない人しか動かさないので、
  画面上でワープして見えることはありません。**フロントは車両と同じく、大きく跳んだら
  補間しないこと**（`samplePedestrian()`）。
- **横断中は車道の中央まで出る。** 歩道の左右を `cross` 0→1 で補間するだけで、
  専用の経路は持ちません。
- ★ **歩行者用の信号は「車両信号の裏返し」で作る。** 横断するエッジを走る車が赤で
  止まっていれば渡ってよい、という 1 対 1 の対応です。地図データにも `frame` にも
  項目を足していません（`SidewalkNetwork._build_signal_table` がエッジの端ごとに
  車両信号を引けるようにしてあります）。**灯器の位置も `map.signals` から導けます**
  （`scene/pedestrianSignalGeometry.ts`）。
- ★ **信号を見るのは渡り「始める」ときだけ。** 渡っている最中に赤へ変わっても
  引き返させません（実際の歩行者と同じく渡り切る）。引き返させると、車道の
  まん中で向きを変える不自然な動きになります。
- ★ **現示を作り直した後に `crowd.step()` へ渡すこと**（`world.advance_time`）。
  1 ステップ古い色で渡らせると、青になる直前に飛び出します。
- **信号の無い交差点では従来どおり自由に渡る。** 銀座では歩道の端 542 か所のうち
  271 か所（50%）にしか信号が結び付きません（元の OSM データに交差点あたり
  数基しか無いため）。実測（銀座・64 人・120 秒）で、赤のうちに渡り始めた回数は
  56 回中 **0 回**、いちばん長い信号待ちは 27.8 秒（サイクルは 60 秒）でした。
- **実用モードでは歩行者の 4.5m 手前で止まる**（`env._pedestrian_gap`）。
  `_lead_gap` と同じく**経路追従の車にだけ掛けること**。学習中の車に掛けると
  「歩行者を轢かない世界」になり、PPO から見た環境が変わってしまいます。
  PPO 側は観測（歩行者の欄と走行可能領域）から自分で止まれるようになります。
  見る相手は `world.pedestrian_xy` で、**NPC 群衆と利用者の徒歩キャラの両方**です
  （実用モードの節の `player_pose`）。
- 実測（銀座 / 金沢・8 台・64 人・真値で走行）: 1 ステップ 17.9ms / 23.5ms
  （歩行者 0 人なら 13.6ms / 19.6ms。予算 50ms）。転送は 5,871B/frame ＝ 114.7KB/s。

### 天候（`percep/weather.py`）

雨と霧は**役割が違い、直交しています**。混ぜないこと。

| | 何をするか | 視程 | 深度バッファ |
|---|---|---|---|
| 雨 | 画を暗く濁らせ、斜めの筋を乗せる | **縮めない** | 要らない |
| 霧 | 距離に応じて白く沈ませる | **縮める** | 要る |

- **視程（`Weather.visibility_m()`）は、擬似カメラの描画と正解ラベルの両方が使う
  唯一の値。** 霧で遠方が白く沈むとき `groundtruth.py` 側も同じ距離で候補を打ち切ります。
  片方だけ変えると「見えないものにラベルが付く」状態になり、認識器から見てタスクが
  定義できません（`camera.py` と `groundtruth.py` の約束と同じ型の壊れ方）
- **深度バッファは霧のときだけ作る**（`Weather.needs_depth`）。晴れ・雨では作らないので
  描画コストは増えません（実測: 晴れ 9.7ms / 雨 13.0ms / 霧 14.5ms、いずれも銀座 8 台）
- **雨の筋は疎な添字で持つ**（`_streak_bank` が `(行, 列, 強さ)` を返す）。
  密な (H, W) マスクで全画素に掛けると、ほとんどが 0 なのに 1 枚 7ms を使います
- `weather_auto` の間、`params` の `weatherRain` / `weatherFog` は**更新しません**。
  いま出ている天候は `frame.weather` で見ること（`docs/protocol.md` 2.3）
- **観測だけが濁り、報酬と終了条件は真値のまま。** 霧で赤信号を見落として進めば、
  そのとおり信号無視として罰が入ります

**3D 側（`scene/weatherView.ts`）にも同じ天候を描きます。** 擬似カメラだけに掛けると、
利用者から見て**天候を切り替えても画面が何も変わりません**（実際にそうなっていました）。
計算は React から切り離した純粋関数に置き、`npm run verify:weather` が数値で確かめます。

- **視程をそのまま 3D のフォグにしないこと。** 視程は「運転席から前を見た距離」なので、
  300m 上空の俯瞰へ適用すると**街が 1 つも見えない真っ暗な画**になります。
  `fogLift()` が視点の高さで緩めます（運転席・追従は視程どおり、俯瞰は遠くが霞む程度）
- **晴れの遠景フォグ（数 km）と霧（数十 m）は幾何補間でつなぐこと**（`blendDistance()`）。
  線形に混ぜると桁が違いすぎて、霧が濃くなるまでほとんど効きません
- **雨を 3D 空間の粒にしないこと。** 俯瞰では 1 画素にも満たず見えません。
  カメラの直前に貼った板へシェーダーで描けば、俯瞰でも運転席でも同じように見えます
- フォグ・明るさ・雨は `useFrame` から `frameBuffer.weather` を読んで**変わったときだけ**
  差し替えます（20Hz の天候を zustand に入れない、という `frame` と同じ扱い）
- **画面に出す天候は `displayedWeather` で、目標値へ追いつく途中の値です。**
  押した瞬間に空と路面が切り替わると作り物に見えるので 1〜2 秒かけて寄せ、
  路面はさらに遅く乾かします（雨がやんだ直後はまだ濡れている）。
  **進めるのは `SceneAtmosphere` の 1 か所だけ**で、`useFrame` の priority を
  負にして最初に走らせています（正にすると R3F が自動レンダリングをやめます）。
  読む側（`Rain` / `SceneLights` / `SunLight` / `RoadNetwork`）は進めないこと
- **濡れた路面で `metalness` を上げすぎないこと。** 環境マップを置いていないので、
  上げても映り込む先が無く**ただ黒くなるだけ**です（0.3 で試して暗いだけでした）。
  粗さを落として日差しの反射を残し、暗くして、空の色を薄く混ぜる形で作ります

**不変条件**:

- **観測はカメラ由来、報酬と終了条件は真値由来。** 認識を誤ればそのまま赤信号に突っ込むが、
  信号無視・衝突の判定は真値で行う。「認識できなかったから見逃す」は起こらない。
  **天候を足しても同じ**（霧で見落としても罰は入る）。
- **失敗を黙って握りつぶさない（`B-15`）。** 認識・衝突・規制速度の判定は、
  失敗すると**成績が良くなる方向**へ倒れます（「前方はすべて空いている」
  「遮蔽は無い」「衝突していない」「規制速度は不明」）。`groundtruth.py`・
  `map/index.py`・`percep/weather.py` の `_warn_once()` が初回だけ記録します。
  **新しく例外を握りつぶすときは必ず同じ形にすること。**
- **推論が落ちたら認識器を手放す（`env._compute_observations` の except）。**
  握ったままにすると `detector_active` が True のままになり、画面の
  「観測の出どころ」が**真値で走っているのに「CNN の認識結果」と出ます**。
  さらに毎ステップ擬似カメラを描いて推論を試み続けるので、落ちた分だけ遅くなります。
  `engine._step_once` は 1Hz でこの値を取り直します（載せ替えのときだけでは
  拾えないため）。復帰は「モデル作成」タブからの読み込み直しです。
- **遮蔽判定は静的（建物）と動的（他車）の 2 本立て。** `_line_of_sight()` は
  占有グリッド（建物レイヤのみ）、`_blocked_by_fleet()` は他車の車体との線分交差。
  後者を適用するのは**車両・障害物・歩行者だけ**で、信号・標識には掛けません
  （車高 1.5m の 2D 判定では、高い位置の灯器や板まで隠してしまうため）。
- **`camera.py` と `groundtruth.py` は同じものを見ていなければならない。**
  描いた物体にラベルが付かない（またはその逆）と、認識器から見てタスクが定義できません。
  正対判定は `percep/types.py` の `facing_viewer()` に一本化してあり、投影式・3D 寸法も
  両者で一致させること。`groundtruth.py` の冒頭にこの約束が書いてあります。
- 3D の寸法は `frontend/src/scene/signalGeometry.ts` / `signGeometry.ts` とも一致させること。
  **片方だけ変えると画面の見た目と検出枠がずれます。**
- `groundtruth._STATIC_CACHE` は `map_index` の identity 比較 1 件だけのキャッシュです。
  **複数マップを行き来するコードでは切り替えのたびに `clear_static_cache()` を呼ぶこと。**

### 実行時のスレッド構成

`runtime/engine.py` が**専用 OS スレッド**で物理と PPO を回し、asyncio（WebSocket）とは
`queue.Queue`（コマンド）とロック付きスナップショット（状態）だけでやり取りします。
だから「一時停止は描画だけ止まり、学習は止まらない」が自然に成立します。

- 1 ステップの予算は 50ms（20Hz）。**エンジンスレッドを止める処理を入れないこと。**
  過去に `_install_route()` が金沢で 1 秒止めていた実例があります（`memo/code_review.md` M-01）。
- PPO の勾配更新は 1 ステップ 1 回ずつに分割済み（まとめると数百 ms 止まる）。

認識器の学習（`runtime/detector_job.py`）は**さらに別のスレッド**で回り、その間だけ
`engine.suspend_sim()` で**物理と PPO を止めます**（`_run_loop` が `_step_once()` を飛ばす）。
利用者の「一時停止」（描画だけ止める）とは別物なので混ぜないこと。止める理由は 2 つ:

1. 収集も学習も CPU を使い切るので、同時に回すと 50ms の予算を守れない
2. `groundtruth._STATIC_CACHE` は `map_index` の identity 比較 1 件だけのキャッシュで、
   別々のマップを持つ 2 つの env が交互に呼ぶと**毎回作り直しになる**

再開（`resume_sim()`）は**inbox 経由**。直前に積む `reload_detector` より先に再開すると、
古い認識器のまま 1 ステップ進んでしまうためです（止めるのは即座、再開は順序つき）。

### 実用モード（自動運転タクシー）

操作パネル右上のトグルで**開発モード**（学習を回す）と**実用モード**（学習済みの重みで
タクシーを呼ぶ）を切り替えます。`runtime/taxi.py` が配車 1 件ぶんの状態を持ち、
契約は `docs/protocol.md` 2.10（`taxi` メッセージ）です。

**止まるものが 3 つあり、どれも別物です**（`docs/protocol.md` 2.4 の表と同じ）:

| | 止まるもの |
|---|---|
| `renderPaused` | フレーム配信だけ。学習は続く |
| `simSuspended` | 物理と PPO ごと（認識器の学習中） |
| `practicalMode` | **重みの更新だけ。** 物理・推論・配信は動き続ける |

不変条件:

- **実用モードの間は全車が PPO ではなく経路追従で走る**（`env.autopilot_all` →
  `env._autopilot_slots`）。手元のモデルが停止方策に寄っていても**必ず迎えに来させる**
  ためで、これが無いと「呼んでも来ない」画面になります（実測: 4,279 更新のモデルは
  銀座で 60 秒走らせても 0〜8m しか動かず、アクセル指令の平均が -0.64 でした）。
  **徴用車 1 台だけでは足りません。** 街の車が止まったままだと道を塞ぎ、
  タクシーがその後ろで詰まります（実測: 全車追従にして 30 秒での移動が
  11〜139m、止まったままの台数 0/6）。信号・規制速度・カーブ・乗降地点での停車は
  `constrain_accel` がそのまま効くので、**赤信号は守るし制限速度も超えません。**
  **開発モードでは必ず `False` に戻すこと**（PPO から見た環境が変わってしまう）。
- **舵角は目標点を通る円弧の曲率から出す**（Pure Pursuit。`δ = atan(L·2sinα/d)`）。
  角度差をそのまま舵角にすると急カーブで切り足りず、膨らんで建物へ突っ込みます
  （実測: 金沢で横ずれが 0.17m → 6.55m に開いて衝突。曲率式にして 4 回 → 2 回）。
  経路から離れているほど目標点を手前に詰めること（離れたまま遠くを見ると戻れない）。
- **曲がっている間は加速しないが、止まっているときは必ず加速する**
  （`AUTOPILOT_CREEP_MPS`）。切ったまま停まると角度差が変わらないので、
  「曲がっているから加速しない」だけだと**二度と発進できません**
  （実測: 銀座で 361 秒動かなくなった）。
- ★ **カーブでブレーキ（負のアクセル）まで踏ませないこと。** 金沢の衝突は
  2 回 → 1 回になりますが、**銀座が 0 回 → 2 回に悪化します**（交差点の中で
  止まってしまうため）。既定のマップは銀座なので入れていません。
- **経路追従には前走車との車間（`env._lead_gap`）が要る。** 経路しか見ないので、
  これが無いと**止まっている車へ必ず追突します**（実測: 赤信号待ちの列に突っ込んで
  「事故が起きたため配車を打ち切りました」で終わりました）。車間も赤信号と同じ式で、
  車長 + 2.5m 手前で止まります。**経路追従の車にだけ掛けること**（学習中の車に掛けると
  「追突しない世界」になり、PPO の学習環境が変わってしまう）。
- ★ **車間を見る相手は「同じ向きに走っている前方 45m 以内の車」だけ**
  （`AUTOPILOT_SAME_WAY_COS` / `AUTOPILOT_LEAD_RANGE_M`）。交差点で直交する車や
  対向車まで前走車として数えると、**互いに相手の前方に居座って双方が永久に止まります**
  （実測で 444 秒）。交差する流れは信号が捌くので見なくてよい。
- **徴用中はエピソードを閉じない**（`env.step` が `dones[held] = False`）。
  閉じると乗降地点に着いた瞬間に respawn し、乗客を置いて別の街区へ飛びます。
- ★ **乗車地点は選ばせない。利用者の現在地が乗車地点です**（実際の配車アプリと同じ）。
  画面で選ぶのは降車地点だけで、`TaxiScreen` が `request_taxi` の `pickup` に
  `store/pedestrian.ts` の座標を入れます。サーバー側の契約は変わりません
  （受け取った点を道路ノードへ寄せる）。`pickup` は**配車 1 件のあいだ動かない
  目標地点**で、呼んだ後に利用者が歩いても追従しません。
- ★ **`approaching` になっても利用者を乗車地点へ動かさないこと**（`Pedestrian.tsx` の
  `placeAtPickup`）。乗車地点は自分の足元なので、立たせ直すと**呼んだ瞬間に自分が
  ワープします**。立たせるのはリロード・再マウントで街から消えているときだけ。
- **乗車は迎車の途中でも受け付ける**（`taxi.board` が `approaching` も通す）。
  乗車地点まで来るのを待たせる理由がないためです（近くで拾えたならそれでよい）。
  経路は必ず**いまいる場所から**作り直す（`route_between(現在地, 降車地点)`）ので、
  途中で乗せても矛盾しません。近いかどうかの判定はフロント側（照準 9m・±0.5rad）です。
- **乗降地点で止めるのは赤信号と同じ式**（`signals.stop_speed_limit`）。
  経路の終点を「越えてはいけない線」として速度上限を掛けるだけで、
  専用の停止ロジックは持ちません（実測の停車精度: 乗車 0.05m / 降車 0.12m）。
- **乗降地点は道路ノードへ寄せる**（`world.snap_to_road_node`。決定 7 の字義どおり）。
  道路中心線上の任意の点にすると、経路はノード間で作られるので
  「目的地を通り過ぎて次の交差点まで行き、折り返して戻る」形になります。
  停車は経路の終点で掛かるため、**目的地の前は減速せずに通過**します
  （実測: 銀座で 64% の経路が平均 8.6m・最大 28.8m 行き過ぎていた。金沢では 72%）。
  `_trim_tail()` は、別の道路から目的ノードへ入る経路のための保険です。
- **配車の経路探索はエンジンスレッドで走る。** 実測 銀座 2.0ms / 金沢 18.8ms（予算 50ms）。
  候補 4 台すべてで経路が作れないと金沢で 0.2 秒ほど止まるので、
  `taxi.ASSIGN_CANDIDATES` を増やすときはここを測り直すこと。
- **モードを切り替えたら配車は必ず畳む**（`_apply_app_mode`）。畳まないと、
  徴用したままの車両が開発モードの学習対象に戻り、経路も停車指示も残ります。
- ★ **迎えに行けなくなった車は、配車を畳まずに別の車へ引き継ぐ**（`taxi._handover`）。
  引き金は 3 つ（事故 / 車両の消失 / **25 秒間まったく近づけない**）。
  `collided_flags` は**当たった瞬間しか立たない**ので、当たらずに詰まって動けない形は
  3 つめでしか拾えません（`_stalled()`。交差点の睨み合いや塞がれた道で実際に起きます）。

  | 守ること | 理由 |
  |---|---|
  | **段階は畳まず `approaching` へ戻す** | 一度でも `idle` を挟むと、フロントは配車が終わったものとして扱う（歩行者を降ろし、画面を初期化する） |
  | **引き継げるのは乗せる前だけ** | `riding` / `arrived` は車内に人がいる。従来どおり打ち切る |
  | **一度あきらめた車は同じ配車の間は選ばない**（`_tried`）| 手放した車の事故フラグは `release_vehicle()` の経路差し替えで**消える**ので、除外しないと 2 台のあいだを往復します（実測で #1 → #5 → #1 → #5）|
  | **上限は 3 回**（`MAX_HANDOVERS`）| 事故が続くと延々と粘ってしまう |
  | **`vehicle_id` も配信の契機にする**（`engine._step_once`）| 段階が変わらないので、1Hz を待つと最大 1 秒古い車を追いかけます |

  引き継ぎ 1 回のコストは**配車と同じ**（`_assign` が `route_between` を最大 4 回）。
  実測 銀座 6.0ms / 金沢 17.2ms で 50ms の予算には収まりますが、
  `ASSIGN_CANDIDATES` を増やすときはここも測り直すこと。
- **徒歩キャラを動かすのはフロントだけ**（`store/pedestrian.ts`）。乗車の可否は
  「照準に入っているか」をフロントが判断して `board_taxi` を送ります。位置は 60fps で
  動くため zustand には入れません（`frameBuffer` と同じ作法）。
- ★ **ただし位置は 10Hz でサーバーへ送ること**（`player_pose`。`docs/protocol.md` 3 章）。
  `World.set_player()` が `pedestrian_xy` へ NPC 群衆と並べて混ぜるので、**これ 1 本で**
  車間・擬似カメラ・正解ラベル・観測・衝突判定の 5 つが同時に利用者を見ます
  （別々に足すと、どれかを足し忘れても**成績が良くなる方向**に倒れて気づけません）。
  送らないと、車は目の前に立っても止まらず、轢いてもすり抜けます。
  実測（銀座・経路追従）: 8.33 m/s から **4.50m 手前で停止**、どかすと再発進。
  1 ステップの増分は銀座 +0.58ms / 金沢 -0.49ms（真値走行 15.9ms / 23.9ms のうち。
  1 人ぶんなので測定誤差に埋もれます）。

  | 守ること | 理由 |
  |---|---|
  | **乗車中（`riding` / `arrived`）は送らない** | サーバーが乗客を歩行者として扱い、**自分を乗せた車が自分の前で止まって動けなくなる**。`engine._apply_player_pose` も `taxi.onboard` で捨てるが、判断は両側に置く |
  | **届かなくなったら 1 秒で消す**（`engine.PLAYER_POSE_TTL_SEC`）| タブを閉じた利用者を街に残すと、見えない人の前で車が永久に止まる |
  | **実用モードを抜けるときは `at: null` を 1 通送る** | TTL 任せだと、開発モードへ戻った直後の 1 秒ぶん PPO の環境が変わる |
  | **`frame` へ返さない** | クライアントは自分のキャラを自分で描いている。返すと二重に描く |
  | **街に立つのは 1 人だけ** | 接続ごとには持たない（配車が同時 1 件なのと同じ前提）。複数タブでは最後に届いた位置が勝つ |
  | **`request_taxi` の `pickup` と混ぜない** | `pickup` は配車 1 件のあいだ動かない目標地点（迎えに行く先）、`player_pose` は 1 秒で消える現在位置（止まる相手）。片方だけにすると「迎えには来るが目の前で止まらない」か「止まるが迎えに行く先が決まらない」になる |
- ★ **`arrived` はまだ乗車中です。** 到着しても徴用は解けず、降車が確定するのは
  `alight_taxi` を受けて `phase` が `idle` へ落ちたとき。**段階から「いま何ができるか」を
  導くのは `types/protocol.ts` の `isRidingPhase()` / `isBoardablePhase()` だけ**で、
  サーバーの `taxi.alight()` / `taxi.board()` が受け付ける段階と一致させること。
  以前は `TaxiScreen` / `TaxiHud` / `Pedestrian` が各自で判定していて、
  **`Pedestrian` だけが `arrived` を含め忘れ**、到着した瞬間に操作もしていないのに
  車外へ降ろされたうえ、画面が案内している `[Enter]` が効かなくなりました
  （`code_review` T-01）。
- **乗っている車のスロットは `pedestrian.ridingVehicle` に覚える。**
  `taxi.vehicleId` は降車が確定した時点で **-1 に戻る**ので、降車位置を出すのに使えません
  （使うと乗車地点へワープします）。

★ **全車を経路追従にしたことで、シミュレータ側の壊れが 3 つ表に出ました。**
どれも実用モード固有ではなく（PPO の車でも観測や事故率が壊れます）、
止まったままの車では気づけなかっただけです。

| どこ | 何が起きていたか |
|---|---|
| `map/index.py` `signals_on_route()` | 同じ交差点の灯器が別ノードに 2 基立っていると、現示は `node_id` からオフセットを決めるので**片方が青でも片方が赤**になり、車はそこから永久に動けません。`SIGNAL_MERGE_M`（8m）以内は 1 基にまとめます |
| `world._forward_node()` | 真横や足元のノードを選ぶと、経路が**その場から直角に始まります**。最大舵角でも旋回半径は 4.4m あるので曲がりきれず、対向車線や建物へ出ます（実測: 乗車直後に横ずれ 0.00 → 3.78m で衝突）。14m 以上・±60° の錐の中から選びます |
| `world._random_route()` | 再配置の始点が他車の 6.6m 先だと、走ってきた車が次の瞬間に追突します。`SPAWN_CLEARANCE_M2` を車 4 台分に広げ、**空いた始点が見つからないときは最初の 1 本ではなく、いちばん空いている候補**を返します |

**金沢では 200 秒・8 台あたり 2 回ほど車どうしの接触が残ります**（銀座は 0 回）。
原因は経路生成側（建物を貫く経路、旋回半径 4.4m より急な曲率）で、実用モードの
問題ではありません。**性能と同じく、ここも銀座だけで見て済ませないこと。**

スマホ画面の地図（`panel/TaxiMap.tsx`）は、配車の段階に合わせて**自動で寄り引きし、
向きも変えます**（`approaching` ならタクシーと乗車地点の両方が入る枠、`arrived` なら降車地点）。
ここも壊しやすい点が 7 つあります:

- **見え方（`MapView`）を `useState` に置かないこと。** 毎フレーム目標へ寄せるので、
  state にすると 60Hz でパネルごと再レンダリングされます（`frame` を zustand に
  入れないのと同じ理由）。`useRef` に置き、描き直すのは Canvas だけにします
- **道路と建物のレイヤは貼り替えて使い回す**（`layerTransform()`）。寄っている最中に
  引き直すと、金沢の 58,120 本で 1 フレームを使い切ります。倍率が 0.16（対数）か
  画面幅の 22% ずれたときと、動きが止まったときだけ描き直します
- **倍率は対数で補間する**（`lerpView()`）。線形に混ぜると 1 倍から 16 倍へ寄るときに
  前半でほとんど動かず、後半で跳ねます（天候のフォグ距離と同じ型）
- **`drawRoads()` は縮尺に対して短すぎる道路を引かない**（既定 2.5px）。
  金沢の全体表示では 1px = 15m なので、路地まで引くと真っ白に潰れるうえに重くなります。
  実測: 金沢の実用モードで中央値 16.7ms（60fps 相当）、**地図の有無で差は出ません**
  （重いフレームは 3D 側の建物 35,607 と標識 15,719 が原因）

★ **車載カメラ（`scene/TaxiCameraFeed`）は、シーンを運転席視点でもう一度描いたものです。**
擬似カメラ（`percep/camera.py` の 192×144）ではありません。**検出枠は
`DetectionOverlay` と同じ `projectBox()`** で写すので、枠の出どころは 1 つです
（`detectionProjection.ts` はカメラの位置に依らず、画角とアスペクトだけで決まる）。

- ★ **オフスクリーン（`WebGLRenderTarget`）へ描くこと。** 画面の一部を借りて描くと、
  そこを元の視点で描き直すためにシーンをもう一度走査することになります
  （カリングと行列更新は解像度に依らないので、小さく描いても安くなりません）。
- ★ **レンダーターゲットの既定はメインの画と揃っていません。** 3 つとも明示すること。
  揃えないと**ギザギザで色の沈んだ「荒い映像」**になります（実際にそうなりました）。

  | | 既定 | 揃える先 |
  |---|---|---|
  | 解像度 | 作ったときの固定値 | **表示の実寸 × dpr**（`feedSizeFor()`。固定にすると拡大されて荒い）|
  | アンチエイリアス | 無し | `{ samples: 4 }`（メインは `antialias: true`）|
  | 色空間 | linear のまま | `texture.colorSpace = SRGBColorSpace`（`outputColorSpace` は**自動で掛からない**）|

- ★ **転送は 15fps に絞る**（`FEED_FPS`）。`readRenderTargetPixels()` は GPU から
  CPU への同期転送で、60fps で回すと描画を待たせます。実測（銀座・モック・809×340 ＝
  1.05MB/枚・15.7MB/秒）で**カメラを出していても 60fps** を保ちました。
  解像度の上限は `FEED_MAX_W` / `FEED_MAX_H`（読み戻しは画素数に比例します）。
- ★ **転送先のキャンバスは借りたら必ず返すこと**（`taxiCamera.canvas = null`）。
  返さないと、畳んだあとも見えない裏で描き続けます。
- **`taxiCamera` は 15fps で書き換わるので zustand に入れない**（`frameBuffer` と同じ作法）。
  zustand に置くのは「出しているか」（`taxiCameraOn`）だけです。
- 映像は**地図へ重ねる**（地図の寸法は変えない）。地図を縮める形にすると、
  開閉のたびに道路・建物のレイヤを描き直すことになります（金沢で 58,120 本）。

★ **向き（`MapView.rotation`）を進行方向へ合わせるのは乗車中（`riding`）だけです。**
迎車中は乗る側が街の中で自分の居場所を掴めるよう北で固定し、到着（`arrived`）したら
北へ戻し始めます。`rotation` は「地図を回す角度」で、進行方向を上にするなら
`rotationForHeading(heading) = heading - π/2`（`rotationForHeading()` が唯一の出典）。

- ★ **静的レイヤは「画面の対角を一辺とする正方形」で描くこと**（`layerSizeFor()`）。
  画面と同じ大きさで描くと、**回して貼った瞬間に四隅が欠けます。** 対角なら
  どの角度でも覆えるので、**向きが変わっても引き直さず貼り替えだけで済みます。**
  実測（金沢・320px → 453px）: 引く道路は zoom 20 で 213 → 342 本に増えますが、
  支配項は 58,120 本の**走査**のほうなので、引き直し 1 回の所要は 4.96ms → 4.58ms と
  変わりませんでした。**引き直しの頻度を上げないことが効きます。**
- ★ **向きは寄り引きより遅く追わせること**（`TURN_RATE` < `FOLLOW_RATE`）。
  車の方位は交差点で一気に変わるので、同じ速さで回すと画面が振られて酔います。
  角度は**近いほうへ回す**こと（`lerpAngle()`。359° → 1° は +2°）。
- ★ **軸ごとの `toCanvasX()` / `toCanvasY()` は廃止しました。** 回転が入ると
  x は y にも依存するので、片方だけでは変換できません。**`toCanvas(p, x, y)` を使うこと**
  （軸ごとの関数を残すと、回っているのに北向きの値が返り、気づけないままずれます）。

### フロントエンドの状態管理

**学習指標の系列も `frame` と同じ扱いです。** `store/simStore.ts` の
`metricsSeries` は**その場で追記する** `number[]` で、読む側は `metricsRevision` を
再計算の契機にします。毎秒 `slice()` して全長のコピーを作ると、24 時間
（86,400 点）で 25.8ms/秒かかっていました（追記式にして 4.4ms/秒）。
**グラフの入力を `map()` で作り直さないこと。**

**20Hz の `frame` は zustand に入れません。** `frontend/src/store/frameBuffer.ts` の
モジュールスコープの mutable オブジェクトに置き、`useFrame` から直接読みます。
zustand に入れると毎秒 20 回ツリーが再レンダリングされて 3D が重くなるためです。
React の再レンダリングは「マップ変更」「パラメータ変更」「メトリクス更新(1Hz)」「UI 操作」だけ。

サーバー 20Hz / ブラウザ 60fps なので、フロント側でフレーム間を補間します。

### 操作パネル（`panel/` / `store/` / `ui/`）

タブは `panel/ControlPanel.tsx` が `key={tab}` で**アンマウントして入れ替えます**。
中身のローカル state は持ち越されないので、実行中の状態を見せたいものは
サーバーから届いたもの（`detector.request` など）を初期値にすること。
実用モードのスマホ画面（`panel/TaxiScreen.tsx`）も同じ枠で `key` を入れ替えるだけで、
**タブ群とは排他**です（決定 9。両方を同時に出さない）。

**`frameBuffer` を読む一覧は、必ず次の 2 つを守ること**（`code_review` F-10 / F-11）:

- `panelOpen` が false の間はポーリングごと止める。**3D を全画面で見たいときにこそ、
  隠れた行の再レンダリングが乗る**
- 中身が変わっていなければ `setState` しない。毎回新しい配列を返すと `Object.is` が
  必ず外れ、停車中でも 4Hz で一覧を丸ごと差分計算することになる

`ui/` は Material 3 Expressive の部品置き場です。**動かしてよいのは transform と
opacity だけ**（出典はこの文書。以前は `ui/motion.ts` の冒頭に書いていましたが、
コメント規約の廃止（2026-09-15）で消えたので**ここが唯一の出典**です）。
パネルは 3D と同じメインスレッドに乗っているので、
幅・高さ・影を毎フレーム変える演出を足すとシミュレータ側のフレームが落ちます。
例外はタブのインジケータ（絶対配置の 1 要素なのでレイアウトが波及しない）。

★ **一度この規約を破っていたので、直した形を残しておきます。**

| どこ | 守ること |
|---|---|
| `Collapse`（`.m3-collapse`）| **高さ（`grid-template-rows`）と margin を遷移させない。** カードの中身を丸ごと包むので、遷移させると開閉のたびにパネル全体が 250ms レイアウトし直されます。高さは 1 回で切り替え、滑らかさは `.m3-collapse-inner` の transform と opacity で出します |
| `ValueFlash`（`restartValueFlash()`）| **`void el.offsetWidth` で巻き戻さない。** 数字は毎秒届き、パネル 1 枚に 9 個あるので、毎秒 10〜15 回の強制リフローになります。`getAnimations()` を**アニメーション名で絞って**時計を戻すこと（名前を見ないと、選択中のチップの拡大まで巻き戻します）。**`requestAnimationFrame` で付け直す形も不可**で、前面でないタブでは rAF が回らず、クラスが外れたまま戻りません |

押した瞬間だけの演出（`startRipple` → `restartAnimation()`）は `void el.offsetWidth` のままです。
pointerdown のときしか走らないので、1 回ぶんのリフローは無視できます。

**バックエンド無しでフロントだけ触るとき**は `npm run dev` で開いて URL に `?mock=1` を
付けます（DEV ビルドのみ。本番では `isMockRequested()` が常に false を返す）。
`store/mockServer.ts` が frame / metrics / network と、認識器の学習の疑似ジョブ、
実用モードの配車（迎車 → 乗車 → 到着 → 降車）まで返すので、
**サーバーを起動せずに見た目と遷移を確かめられます。** 実物のモデルが無いので
書き出し・読み込みだけは塞いであります。

### PWA（`frontend/public/`）

インストールして全画面で使えるようにしてある。**ライブラリは足していない**
（`vite-plugin-pwa` も workbox も無し）。`manifest.webmanifest` と `sw.js` は手書きで、
アイコンは `scripts/make-icons.ts` がファビコンと同じ図形を数式で描いて PNG にする。

`sw.js` の不変条件（破ると気づけない形で壊れる）:

- **`/api/` と GET 以外には触らない。** モデルの書き出しは数十 MB、読み込みは
  アップロード。挟むだけで進捗が壊れ、キャッシュすれば古い重みを配る
- **HTML はネットワーク優先。** `index.html` は名前が固定で中身（参照する
  `index-<ハッシュ>.js`）が毎ビルド変わる。キャッシュを先に返すと
  **消えた JS を指したままの画面**が出る。`app/main.py` の `_FrontendStatic` が
  同じ理由で `no-cache` を付けており、その対策を SW 側で台無しにしないこと
- **`CACHE_VERSION` は中身を変えたら上げる**（`map/loader.py` と同じ約束）
- `sw.js` 自身と `manifest.webmanifest` はサーバーが `no-cache` で返す
  （`_NO_CACHE_FILES`）。ここをキャッシュさせると**キャッシュ規則を直しても
  永久に効かない**ので、SW も manifest を `/api/` と同じ素通しにしてある
- **`cache.put()` は必ず `event.waitUntil()` に包む。** await せずに返すと、
  ハンドラが返った後にブラウザが SW を止めて**書き込みが打ち切られる**。
  症状は「オフラインで開くとシェルが無い」という再現性の低い形で出る
- **`/assets/` は件数に上限を設ける**（`ASSET_KEEP`）。キャッシュ優先で
  無条件に入れるので、再ビルドのたびにハッシュ違いの JS/CSS が積み上がる。
  three.js と drei で 1 バンドル 1MB を超えるため、放っておくと数十 MB になる
- **プリキャッシュする `SHELL` のアイコンは `manifest.webmanifest` の宣言と揃える**
  （3 枚とも `scripts/make-icons.ts` が同時に作る）
- `manifest.webmanifest` の `theme_color` は**インストール時に OS が使う唯一の色**で、
  `index.html` の 2 つ（昼夜）は参照されない。1 色しか持てないので中間色を置いてある

登録は `store/pwa.ts` が**本番ビルドでのみ**行う。開発中（Vite）は登録せず、
前回の登録が残っていれば剥がす（SW が HMR を殺すため）。

### 車両のライト（`scene/vehicleLights.ts`）

前照灯・尾灯・制動灯・方向指示器の 8 灯を、**1 つの `instancedMesh`（8 台 × 8 灯 ＝ 64）**
で描きます。点灯は `instanceEmissive`（`Vehicles.tsx` の `attachInstanceEmissive`）で、
灯体そのものの色は消灯時も残します。

**決めるのを 2 か所に分けてあります。混ぜないこと。**

| | どこで決めるか | なぜ |
|---|---|---|
| 制動灯・方向指示器 | **サーバー**（`frame.vehicles[].braking` / `turnSignal`）| 制動は「指令が負か」で、経路の先読みは経路を持つ側にしかできない |
| 前照灯・ハザード | **クライアント** | 昼夜（`store/themeClock.ts` の日の出・日の入り）と配車の段階（`taxi.phase`）はクライアントにしか無い |

- ★ **制動灯は加速度の実測ではなく指令で点けること**（`world.set_braking`）。
  実測から出すと、エンジンブレーキや空気抵抗で減速しただけでも点きます。
  しきい値 `BRAKE_COMMAND_THRESHOLD`（-0.05）を 0 にすると、方策が出す微小な負の値で
  点きっぱなしになります。
- ★ **方向指示器にはヒステリシスが要る**（出す 0.35rad / 消す 0.12rad）。
  片側のしきい値だけだと、緩いカーブを曲がっている間じゅう点いたり消えたりします。
  実測（銀座・経路追従 8 台・60 秒）で状態の変化は 0.55 回/秒、
  点灯していた割合は左 9.6% / 右 16.3%、制動灯 8.3% でした。
- ★ **ハザードは方向指示器より優先すること**（`lightStateFor`）。乗降を待っている車が
  右ウインカーだけ出していると、外から見て発進するように見えます。
  段階の判定は `types/protocol.ts` の `isWaitingPhase()` だけに置くこと
  （各自で `phase === 'waiting'` と書くと、`arrived`（降車待ち）を落とします。`code_review` T-01）。
- **点滅は毎分 60〜120 回**（保安基準）。`BLINK_HZ` は 1.5（毎分 90 回）で、
  位相はクライアントが `performance.now()` から作ります（20Hz で送るとカクつく）。
- **明るさが変わったときだけ GPU へ送る**（`lastLights`）。1.5Hz の点滅を毎フレーム
  送ると、8 台 × 8 灯 × 3 float を 60fps で書き続けることになります。

### ナンバープレート（`scene/licensePlate.ts`）

日本の中型・自家用（白地に緑文字、330 × 165mm）を前後に 1 枚ずつ。
文字は**車両ごとに違う**ので、**8 台ぶんを縦に並べた 1 枚のテクスチャ**を作り、
UV の v をずらして「その車の段」だけを貼ります（`attachPlateAtlas`）。

- ★ **`instancedMesh` は 1 つのテクスチャしか持てない。** 車ごとに `map` を差し替える
  ことはできないので、アトラスと UV ずらしが要ります。`instancePlateRow` を
  頂点シェーダーで読み、`vMapUv.y` を段の範囲へ写しています。
- ★ **Canvas は上から 0, 1, 2 … と描くが、テクスチャの v は下から数える。**
  `plateUvRow()` がこの反転を引き受けます（直接 `id` を段にすると、車 #0 に #7 の
  番号が出ます）。
- ★ **テクスチャは `resources`（ジオメトリ一式）とは別の `useEffect` で作ること。**
  地名は走っている街に合わせるので、プリセットを変えるとテクスチャだけ作り直します。
  ジオメトリごと作り直すと `instancedMesh` が `count=0` に戻って車が消えます。
- **一連指定番号は上位を中黒で埋め、4 桁のときだけハイフンを挟む**（`plateSerial()`）。
  中黒は数字より小さく詰めて描くこと。等幅で並べると「・ ・ ・ 0」と間延びして
  日本のプレートに見えません。
- **擬似カメラには描いていません。** プレートは 33cm で、192×144 の擬似カメラでは
  1 画素にも満たないためです（検出クラスは 6 種のまま）。
- ★ **実用モードの画面は車をプレートで指す**（`plateLabel()`）。サーバーの文言に含まれる
  `車両 #N` は `withPlateNames()` が差し替えます。**番号を文中に持つのはサーバー側**
  （引き継ぎの「#1 が来られなくなったため #5 が」は、どの車からどの車へかを知っている
  側でしか作れない）で、地名の対応表はクライアントにしかないため、この分担になりました。
  書式は `docs/protocol.md` 2.10 に書いてあります。
  **開発モードの一覧・カメラ選択は番号のまま**です（そちらは識別が目的で、
  33cm のプレートより番号のほうが読み取りやすい）。

### 3D 描画で壊しやすいところ

- **`polygonOffset` の重ね順**: 路面 -1/-2 ＜ 中央線 -2/-4 ＜ 白線 -3/-6 ＜ リボン -4/-8
  ＜ 矢羽根 -5/-10 ＜ 認識車線 -6/-12。崩すと Z ファイティングで点滅します。
- **配色は `scene/palette.ts` が単一の出典**（`usePalette()` 経由）。昼と夜の 2 組を持ち、
  走らせている街の**日の出・日の入りで自動的に切り替わります**（`store/autoTheme.ts`。
  設定項目は無い）。信号の灯火と標識の板の色だけは昼夜で変えません。
- **palette を `<instancedMesh args={[...]}>` の依存にしないこと。** R3F は `args` を
  要素ごとに比較し、違えばオブジェクトごと作り直します。作り直された `InstancedMesh` は
  `count={0}` に戻るのに行列を書く `useEffect` は走らないので、**配色が切り替わった瞬間に
  物体が消えます**。マテリアルは作り直さず色だけ差し替え、`args` に渡したものは
  行列書き込み側の deps にも入れること（`Vehicles.tsx` が手本）。

### 方策が「止まる」に固まるのを防ぐ

★ **行動分布の平均（`policy._distribution` の `mu`）は必ず `tanh` で -1..1 に収めること。**
`PPOTrainer.act` は `np.clip(raw, -1, 1)` してから環境へ渡すので、**環境から見ると
平均が -1 でも -3 でも同じ**（どちらもフルブレーキ）です。平均に制約が無いと、
その区別のつかない方向へ勾配が流れ続けても損失は下がり、**平均だけが範囲の外へ
遠ざかって二度と戻れなくなります。**

実際にこれで壊れました（実測。銀座・更新 4,581 回）:

| | 直す前 | `tanh` を入れた後 |
|---|---|---|
| クリップ前の平均（アクセル） | **-1.710** | -0.529 |
| 平均が -1 より下＝ 3σ 足しても正になれない割合 | **80.2%** | **0.0%** |
| 60 秒での移動 | 0〜3m（8 台中 5 台は微動だにしない） | — |

**報酬設計は無実でした。** 同じ状況で測ると経路追従で走れば 60 秒で +168.5、
止まったままなら -60.0 で、**走る方が 228 ポイント得**です。それでも止まっていたのは、
方策が「アクセルを正にする」選択肢を構造的に失っていたからです。
**罰の重さを疑う前に、まず平均が範囲の中にいるかを見ること。**

`tanh` は**再発を防ぐだけで、固まった重みは戻しません**（入れた直後に 60 秒走らせても
0〜3m のまま）。回復には `warmstart_policy.py` を使います。

```powershell
.venv\Scripts\python.exe warmstart_policy.py --preset ginza
```

実用モードの経路追従（`env._autopilot`）が出す操作をそのまま教師にします
（`rl/warmstart.py`。行動クローニング）。

★ **直すのは 3 つセットで、1 つでも欠けると走り出した直後に崩れます。**
最初に方策だけを回帰したときは、**たった 2 更新（4621 → 4623）でアクセル指令が
+0.75 から -0.81 へ吹き飛びました。**

| 直すもの | 欠けるとどうなるか |
|---|---|
| **方策の平均**（`fit_policy`） | そもそも走り出さない |
| **価値関数**（`fit_value`） | **これが主犯。** 「止まる前提」で学習済みの価値を残したまま走り出すと、予測と実測が大きく食い違い、その誤差がそのまま advantage に乗って 1〜2 更新で方策を壊す |
| **探索ノイズ**（`set_exploration`） | 既定の std 0.6 はステアに乗って車線を外し、衝突の罰（-100）で「止まる方が得」へ引き戻される。**ただし絞りすぎも損**（下記） |

切り分けの実測（銀座・60 秒・8 台。いずれもウォームスタート直後から PPO を回した）:

| | 移動/台 | 報酬 | 衝突 | 価値予測 |
|---|---|---|---|---|
| 方策だけ・std 0.6 | 142m | +61.2 | 1 | **+0.1** |
| 方策だけ・std 0.15 | 165m | **+138.4** | **0** | +0.3 |
| 方策 + 価値・std 0.6 | **250m** | +78.4 | 3 → 120 秒で **8** | **+12.7** |
| **3 つとも**（本番の壊れた重みから） | 191m | **+107.0** | 2 | **+14.2** |

価値関数は**走る方向への推進力**を取り戻し（移動が伸びる）、ノイズ絞りは**衝突を止めます**。
役割が違うので両方要ります。教師のリターン平均が +15.7 なので、価値予測が
**ひと桁台なら回帰できていない**と判断してよい。

★ **探索ノイズ（`WARMSTART_STD`）は絞りすぎないこと。** 最初 0.15 にしたところ、
衝突は止まりましたが**学習の伸びが鈍り、エントロピーが -0.96 に張り付いて
画面上は「学習が動いていない」ように見えました**（エントロピーは `log_std` だけで
決まるので、固定すると本当に動きません）。60 秒あたりの報酬の実測は
**0.6 が +61〜+78 / 0.15 が +148 / 0.37 が +126〜+264** で、0.3〜0.4 が釣り合います。

**エントロピーが下がること自体は異常ではありません。** `README.md` の「健全なら 1.8 前後」は
初期値 std 0.6 に対応する数字です。学習が進めば下がるのが自然で、
警戒すべきは**上がりすぎ**（4.8 付近＝方策が実質ランダム）の側です。

復旧の実測: 方策 0.183 → 0.003 / 価値 304.9 → 79.7、**60 秒での移動が 0〜3m から
113〜488m**、止まったままの台数 5/8 → 0/8、アクセル指令 -0.82 → +0.83。
その後 PPO を 18 更新続けても健全（報酬 +128〜+247、ゴールも出る）。

- **リターンはエピソード境界で割引を切る**（`collect_expert`）。跨いで積むと、
  respawn 後の報酬を前のエピソードの価値として教えることになります
- **教師の行動は ±0.98 で頭打ちにする**（`TEACH_LIMIT`）。`tanh` は ±1 に決して
  届かないので、±1 を教えると損失が下がりきらず延々と引っ張られます
- **終わったらロールアウトを必ず捨てる**（`trainer.reset_rollout()`）。PPO は方策オンなので、
  回帰前の方策で集めた経験をそのまま食わせると比が壊れます
- 元の重みは `shared_policy.pt.before-warmstart` へ自動で控えます（`--no-backup` で無効）
- **サーバーを止めてから実行すること。** 動かしたままだと自動保存と取り合いになり、
  Windows では `PermissionError`（`os.replace` が失敗）になります。
  **止め忘れると、直した重みをサーバーが数更新で壊して上書きします**（実際にやりました）

### モデルの入出力

- **チェックポイントは必ず `weights_only=True` で読む。フォールバックしない。**
  `rl/importer.py` がこれを宣言しており、`SECURITY.md` にも書いてあります。
- 書き出し（`.pt` / TorchScript / `.keras`）には観測レイアウトと行動スケールを
  メタデータとして必ず埋めます。無いと受け取った側が 57 次元に何を入れるか分かりません。
- 観測次元を変えると過去のモデルは読み込めなくなります（50 → 54 → 56 → 57 → 66 の履歴あり。
  66 は歩行者の欄を足したとき）。

### マップのキャッシュ

`map/loader.py` の `CACHE_VERSION`（**値はその定数が唯一の出典**）を
**生成物の中身を変えたら必ず上げること**。上げないと古いキャッシュが読まれ続けます。

同じ約束を持つ版番号が他に 3 つあります。**どれも「中身の意味を変えたら上げる」**です。

| 版番号 | どこ | 何を守るか |
|---|---|---|
| `CACHE_VERSION` | `map/loader.py` | OSM の正規化済みキャッシュ |
| `CACHE_VERSION` | `frontend/public/sw.js` | PWA のアプリシェル |
| `CHECKPOINT_FORMAT` | `rl/ppo.py` | 学習済みの重み |
| `DATASET_VERSION` | `percep/trainer.py` | 認識器の教師データ `.npz` |

`DATASET_VERSION` は 3 です（1 → 2 は教師データに `weather` 列を足したとき、
2 → 3 は歩行者クラスを足して検出チャンネル数が 23 → 24 に変わったとき）。
後から足したものです（`code_review` D-05）。無かった頃は、
検出チャンネル数だけが変わったときに `make_detection_loss` の
`y_true[..., off : off + size]` が**縮んだスライスを黙って受け取る**ので、
「学習は最後まで走ったのに、まったく検出しないモデルができる」という
原因にたどり着けない失敗をしました。保存は `collect_dataset` の戻り値に
`dataset_meta()` を混ぜる形で 1 か所に寄せてあり、読込は `load_dataset()` が照合します。

### 生成物と環境変数

`backend/data/` 配下は**すべて生成物**です（`.gitignore` 済み。`config.py` が起動時に作る）。
OSM キャッシュ・チェックポイント・認識器と教師データ・書き出し・アップロードが入ります。
**消してよい**ですが、マップの再取得（Overpass。初回 10〜60 秒）と学習のやり直しが要ります。

`.env`（任意。`.env.example` を複製する）で `DRIVERL_HOST` / `DRIVERL_PORT` を変えられます。
**ポートを変えたら `frontend/vite.config.ts` のプロキシ先も直すこと。**

---

## プリセットの規模差に注意

銀座・梅田・栄は 0.8〜0.9km 四方ですが、**金沢だけ 12.3km 四方で 2 桁違います**
（ノード 19,493 / 標識 15,719 / 建物 35,607）。性能に関わる変更は**必ず金沢でも測ること。**
銀座で 2ms の処理が金沢で 1 秒になる、という差が実際に出ます。

金沢は「経路の 80% が物理的に到達不能」（無作為な 2 点が平均 6km 離れるのに 1 エピソードで
2,780m しか進めない）という既知の問題を抱えており、学習題材としては銀座を使うのが既定です。

---

## 作業の進め方

### 確認を取ってから行うこと

次の操作は、**何をなぜ実行するかを伝えて承認を得てから**行ってください。

- ファイル・ディレクトリの削除
- `git push` / 履歴を書き換える操作（`rebase` / `reset --hard` / `commit --amend`）/ ブランチの削除
- `backend/data/` を消すこと（学習済みの重み・認識器・マップキャッシュが入っている。
  消すと Overpass からの再取得と学習のやり直しが要る）
- **依存ライブラリの追加。** このプロジェクトは**ライブラリを足さない**判断を随所でしています
  （PWA は `vite-plugin-pwa` も workbox も使わず手書き、グラフも自前の SVG、
  アイコンは Node 標準の zlib だけで生成）。足す前に必ず相談すること

利用者から明示的に頼まれた操作には応じてかまいません（その場合も、危険な操作は内容を示して確認する）。

### git

- 作業は**トピックブランチ**で行い、`main` へ直接コミットしない。
  命名は既存に合わせて `feat/...` / `fix/...`（例: `feat/detector-training-tab-and-pwa`）
- コミットメッセージは `feat:` / `fix:` + **日本語**の要約。本文には
  **「なぜそうしたか」と「★ 壊れやすい点」**を書く（既存のログが手本）
- 利用者が作業完了（機能の実装やバグ修正の完了）を報告したら、
  **プライバシーの確認**（個人情報・鍵・ローカルの絶対パスはプレースホルダーへ）をしたうえで
  プルリクエストを出す。既存ブランチの再利用でかまわない
- PR・コミットにセッション URL を書かない

### 同時に走らせるエージェント

作業の複雑さに応じて分けてください。**同時に 3 体まで。**

---

## コードの書き方

- **コメントは最小限。** 「何をしているか」を言い直すコメントは書かない。
  名前と型で読めることはコメントにしない
- 残してよいのは次の 3 つだけ:
  1. Python の docstring（**1 行**。空にすると本体が消えて `SyntaxError` になる箇所がある）
  2. TypeScript の `/** ... */`（**1 行**。公開 API の説明）
  3. 指令コメント（`# noqa` / `# -*- coding` / `// @ts-` / `// eslint-` など）
- **コメント・ドキュメント・利用者とのやり取りはすべて日本語。** 変数名・識別子は英語
- 設計の意図・経緯・実測値は**コードではなくこの文書と `README.md` に書く。**
  以前はコードのコメントに書き込むスタイルで、`★` を「ここを変えると気づけない形で
  壊れる」印として使っていましたが、**その規約は廃止しました**（2026-09-15）。
  いま残っている不変条件はすべてこの文書の側にあります
- **失敗を握りつぶすときは必ず初回だけログを残す。**
  黙らせると「衝突しない世界」「速度超過 0 件」のように**成績が良くなる方向**に症状が出て、
  外から絶対に気づけなくなります（`code_review B-15`）
- `memo/code_review.md` に既知の指摘が番号付き（`B` / `F` / `X` / `P` / `Q` / `R` / `W` / `L` /
  `M` / `C` / `S` / `D` / `A` / `U`）で溜まっています。該当箇所を直すときは番号を引いて参照すること。
- ★ **`memo/` は `.gitignore` されています**（`.gitignore` の `/memo`）。開発者の手元にしかありません。
  クローンしただけの環境には、`memo/code_review.md`（指摘の蓄積）も
  `memo/memo_x.x/memo_1.0.md`（**コード中の「memo 5章」の出典**。`contracts.py` /
  `map/loader.py` / `runtime/engine.py` の docstring から参照されている。
  件数は `grep -rn "memo [0-9]章" backend/app` が出典）も
  存在しません。**手元に `memo/` が無ければ、その参照は追えません。**
  その場合は `README.md`「設計上の要点」と、この CLAUDE.md の不変条件を一次情報としてください。

### 数字を文書に手で書くとき

行数・件数・本数のような**手で書いた数字は必ず古くなります。**
実際に「検証スクリプトの本数」が `package.json`（8 本）・`CLAUDE.md`（7 本）・`README.md`（4 本）で
三つ巴にずれていた実績があります。書くなら**出典（唯一の正）を併記**し、
増減させたときに直す場所を明示してください。
