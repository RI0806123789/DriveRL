# セキュリティポリシー

## 前提となる利用形態

DriveRL は **手元のマシンで単一ユーザーが動かす** ことを前提に作られています
（`memo/memo.md` 5章「マルチユーザー対応: 単一ユーザー想定」）。

そのため、次のものを **実装していません**。

- 認証・認可（誰でも接続できれば誰でも操作できる）
- セッション管理・利用者の識別
- レート制限・リクエスト数の制御
- WebSocket / HTTP の暗号化（TLS）

既定では `127.0.0.1:8000` にのみ待ち受けるので、同じマシンからしか接続できません。
**この既定を変えないでください。** `DRIVERL_HOST` を `0.0.0.0` にすると、同一ネットワーク上の
誰でも操作でき、後述のリスクがそのまま外部に開きます。

---

## 想定しているリスクと対策

### 1. モデルファイルの読み込み（`POST /api/import`）

PyTorch の `torch.load` は既定で pickle を実行するため、細工された `.pt` ファイルは
**任意コード実行の入口**になり得ます。これは PyTorch 全般に共通する既知のリスクです。

本アプリの対策:

- アップロードされたファイルは必ず `torch.load(weights_only=True)` で解析します。
  テンソルと素の Python 値以外が含まれていれば、その時点で読み込みを中止します
  （`backend/app/rl/importer.py`）。
- **重みを実際に載せる `PPOTrainer.load()` も `weights_only=True` で読みます**
  （`backend/app/rl/ppo.py`）。検証と読み込みで同じファイルを 2 回開くので、
  片方だけ安全でも意味がありません。安全モードで読めないファイルは理由を
  ログに出して拒否し、`weights_only=False` へフォールバックすることはありません。
- **ファイル形式の見分けにもデシリアライズを使いません。** `.keras`（zip）と
  TorchScript（zip）は中身のファイル名一覧だけで判別します
  （`backend/app/rl/importer.py`）。以前は取り違えを親切に案内するために
  `torch.jit.load()` を呼んでおり、**上の宣言の唯一の例外**になっていました。
- ファイル名はディレクトリ区切りや `..` を取り除いてから保存します
  （`backend/app/main.py` の `_safe_upload_name`）。保存先を抜け出すことはできません。
- アップロードサイズの上限は 256MB です。超えた時点で書き込みを打ち切ります。

それでも、**出所の分からないモデルファイルは読み込まないでください。**
読み込むのは自分が書き出したファイルか、信頼できる相手から受け取ったものだけにしてください。

### 2. 外部への通信

地図データの取得のみ、OpenStreetMap の Overpass API へ HTTPS で接続します。
それ以外に外部へ送信するデータはありません。学習データもモデルも、すべてローカルに留まります。

### 3. ディスクへの書き込み

サーバーは以下にファイルを書きます。いずれもリポジトリ内で完結し、`.gitignore` で除外されています。

| 場所 | 内容 |
|---|---|
| `backend/data/osmnx_cache/` | OSM の生レスポンス |
| `backend/data/map_cache/` | 正規化済みマップ（JSON） |
| `backend/data/checkpoints/` | 学習済みモデル（自動保存） |
| `backend/data/exports/` | 書き出したモデル |
| `backend/data/uploads/` | 読み込みのためにアップロードされたモデル |
| `backend/data/detector/` | 画像認識器（`detector.keras`）と教師データ（`dataset/`）|

`exports/` は最新 20 世代だけ残し、それより古いものは書き出しのたびに自動で消します
（`backend/app/rl/export.py` の `MAX_EXPORT_FILES`）。
`uploads/` は読み込みが終わった時点で削除します。失敗した場合も、サイズ超過で
打ち切った部分ファイルも含めて必ず消します（`backend/app/main.py` の `/api/import`）。
読み込み前の重みは `exports/` に `before-import` として退避されるので、
`uploads/` を消しても取り戻せます。

`osmnx_cache/` と `map_cache/` は自動削除しません。地図の再取得には
Overpass API へのアクセスが要るため、意図的に残しています。

`detector/` は「モデル作成」タブ（`start_detector_training`）か
`backend/train_detector.py` を実行したときだけ書き換わります。**学習が完了した
ときにだけ上書きし、中断したモデルは保存しません**（中途半端な重みで既存の
認識器を黙って悪くしないため）。世代管理はしていないので、**残したい認識器は
自分で退避してください。**

### 4. WebSocket 経由の操作

`/ws` に接続できれば、マップの切り替え・パラメータ変更・学習済みモデルの保存と読み込み・
**ポリシーの初期化（学習成果の破棄）**・**画像認識器の再学習**まで、すべて実行できます。
認証はありません。ローカル限定で動かす前提が守られていれば問題になりませんが、
公開すると誰でも学習を壊せることを理解しておいてください。

認識器の再学習（`start_detector_training`）は**数分間 CPU を占有し、完了すると
`detector.keras` を上書きします**。実行中はシミュレーションと強化学習が止まります
（同時に走らせると両方が遅くなるため）。値域の検証はしていますが、
**繰り返し投げられれば学習を止め続けられる**ことに変わりはありません。

### 5. 依存ライブラリ

固定の仕方がバックエンドとフロントエンドで違います。

| 対象 | 固定の仕方 | 再現インストール |
|---|---|---|
| `requirements.txt` | `==` で完全固定 | `pip install -r requirements.txt` |
| `frontend/package.json` | `^` の範囲指定（マイナー・パッチが上がりうる）| **`npm ci`**（`package-lock.json` の内容を厳密に再現）|

フロントエンドで `npm install` を使うと、`package-lock.json` が更新されて
実際に入るバージョンが変わることがあります。**同じ環境を再現したいときは `npm ci` を
使ってください。** 更新するときは変更内容を確認してください。フロントエンドの依存は
`npm audit` で既知の脆弱性を確認できます（導入時点で 0 件）。

---

## やってはいけないこと

- **公開サーバーで動かさない。** 認証も TLS もないため、そのまま外部に出すと
  誰でも操作でき、`POST /api/import` を悪用される余地が生まれます。
- **`DRIVERL_HOST` を `0.0.0.0` にしたまま放置しない。**
- **信頼できないモデルファイルを読み込まない。**

どうしてもローカル以外から使いたい場合は、SSH ポートフォワードや VPN のように
**アプリの外側で認証と暗号化を用意して**ください。アプリ自体には
その機能を追加していません（単一ユーザー想定という設計判断のためです）。

---

## 脆弱性の報告

セキュリティ上の問題を見つけた場合は、**Issue ではなく**リポジトリの
「Security」タブ →「Report a vulnerability」から非公開で報告してください。

そちらが利用できない場合は、作者（<https://github.com/RI0806123789> のプロフィール記載の手段）へ
直接ご連絡ください。

報告の際は、再現手順・影響範囲・確認した環境（OS / Python / Node のバージョン）を
添えていただけると助かります。

なお本ソフトウェアは学習・研究目的の個人プロジェクトであり、
LICENSE.md の通り**無保証**で提供されます。対応をお約束できるものではありません。

---

## サポート対象

| 対象 | 状況 |
|---|---|
| `main` ブランチの最新版 | 対応する |
| 過去のコミットやフォーク | 対応しない（最新版へ更新してください）|

